version: '3.1'

services:
  db:
    container_name: database
    image: sameersbn/postgresql:10-2
    environment:
       POSTGRES_DB: "boardFriends"
       POSTGRES_USER: "postgres"
       POSTGRES_PASSWORD: "Dionis0799"
    restart: always
    ports:
      - "5432:5432"

  flask:
    container_name: api
    build:
        context: ./app
    command: python main.py
    restart: always
    network_mode: "host"
